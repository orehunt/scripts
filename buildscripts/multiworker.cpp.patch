--- src/workers/MultiWorker.cpp.orig	2018-12-20 11:03:29.672529468 +0100
+++ src/workers/MultiWorker.cpp	2018-12-20 11:03:20.690375682 +0100
@@ -24,6 +24,7 @@
 
 
 #include <thread>
+#include <unistd.h>
 #include <log/Log.h>
 
 
@@ -103,6 +104,7 @@
 void MultiWorker::start()
 {
     size_t memory = scratchPadMem.realSize > ONE_MB ? scratchPadMem.realSize / ONE_MB : scratchPadMem.realSize / 1024;
+    const int cpuSleep = Options::i()->cpuSleep();
 
     if (Options::i()->colors()) {
         LOG_INFO(WHITE_BOLD("Starting thread ") GREEN_BOLD("%zu/%zu") " affined to core: " GREEN_BOLD("#%d") " -> huge pages:" GREEN_BOLD(" %s%zu/%zu") " scratchpad: " CYAN_BOLD("%zu.0 MB"),
@@ -140,6 +142,9 @@
                 *Job::nonce(m_state->blob + i * m_state->job.size()) = ++m_state->nonces[i];
             }
 
+            if (cpuSleep >= 0) {
+              usleep(cpuSleep);
+            }
             CryptoNight::hash(m_hashFactor, Options::i()->asmOptimization(), m_state->job.height(), m_state->job.powVariant(), m_state->blob, m_state->job.size(), m_hash, scratchPads);
 
             for (size_t i=0; i < m_hashFactor; ++i) {
@@ -205,4 +210,4 @@
 
 Worker* createMultiWorker(Handle *handle, size_t hashFactor) {
     return new MultiWorker(handle, hashFactor);
-}
\ No newline at end of file
+}
